# VS Buddy Environment Configuration
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: Never commit .env.local to version control

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# PostgreSQL Database URL with pgvector extension
# Format: postgresql://user:password@host:port/database?param=value
#
# Examples:
# - Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# - Neon: postgresql://[USER]:[PASSWORD]@[ENDPOINT].neon.tech/[DATABASE]?sslmode=require
# - Local: postgresql://postgres:password@localhost:5432/vsbuddy
#
# IMPORTANT: Ensure pgvector extension is enabled in your database:
# CREATE EXTENSION IF NOT EXISTS vector;
#
# Connection Pooling Parameters (append to DATABASE_URL):
# - connection_limit=10     Max connections (10 for serverless, 20+ for traditional servers)
# - pool_timeout=30         Max seconds to wait for a connection from the pool
# - connect_timeout=30      Max seconds to wait for initial connection
#
# Example with pooling:
# DATABASE_URL=postgresql://user:password@host:5432/vsbuddy?connection_limit=10&pool_timeout=30&connect_timeout=30
DATABASE_URL=postgresql://user:password@host:5432/vsbuddy

# NextAuth.js Secret
# Generate a secure secret: openssl rand -base64 32
# This is used for JWT signing and encryption
AUTH_SECRET=your-super-secret-auth-key-generate-with-openssl

# ============================================================================
# OPTIONAL VARIABLES (with defaults)
# ============================================================================

# Default chat model for completions
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
# Default: gpt-4o-mini
# DEFAULT_CHAT_MODEL=gpt-4o-mini

# Default embedding model
# Options: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
# Default: text-embedding-3-small
# DEFAULT_EMBEDDING_MODEL=text-embedding-3-small

# Temperature for chat completions (0.0 = deterministic, 2.0 = very random)
# Default: 0.7
# DEFAULT_TEMPERATURE=0.7

# Number of chunks to retrieve for RAG context
# Default: 8
# DEFAULT_TOP_K=8

# Minimum similarity threshold for chunk retrieval (0.0 to 1.0)
# Default: 0.7
# MIN_SIMILARITY=0.7

# Node environment (development or production)
# Default: development
# NODE_ENV=development

# Sentry DSN for error tracking (optional)
# Get your DSN from: https://sentry.io/
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ============================================================================
# SEED SCRIPT VARIABLES (optional)
# Used when running: pnpm prisma db seed
# ============================================================================

# Default admin user credentials
# IMPORTANT: Change these in production!
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=changeme123
# ADMIN_NAME=Admin

# ============================================================================
# PRODUCTION NOTES
# ============================================================================
#
# For production deployments:
# 1. Generate a strong AUTH_SECRET: openssl rand -base64 32
# 2. Use a managed PostgreSQL service with pgvector (Supabase, Neon, etc.)
# 3. Change default ADMIN_PASSWORD immediately after first login
# 4. Consider using environment-specific .env files (.env.production, etc.)
# 5. Enable connection pooling for DATABASE_URL if needed
# 6. Monitor OpenAI API usage and set billing limits
